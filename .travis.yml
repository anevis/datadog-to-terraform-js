language: bash

services:
    - docker

jobs:
    include:
        - stage: 'Tests'
          name: 'Unit Tests and Analyses to identify vulnerabilities'
          script: ./ci.sh test
        - stage: Build
          script: ./ci.sh build
          if: branch != master
        - stage: Build & Deploy
          script: ./ci.sh build
          if: branch = master
          deploy:
              provider: pages
              skip_cleanup: true
              github_token: $GITHUB_TOKEN # Set in the settings page of your repository, as a secure variable
              keep_history: true
              local_dir: build/
